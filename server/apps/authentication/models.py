from django.db import models

# Create your models here.

# Defines the User model if custom fields are required beyond Django's default User model.
# It may include additional methods for user-related processes.
